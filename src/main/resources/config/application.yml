server:
  port: 8080
  servlet:
    context-path: /parameter-api
    session:
      cookie:
        path: /parameter-api
spring:
  profiles:
    active: test
  data:
    mongodb:
      host: localhost
      port: 27017
      database: test